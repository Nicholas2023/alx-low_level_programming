#include <stdio.h>

/**
 * main - Entry point
 * Description: Prints a program's name
 * @argc: Number of arguments in the command line
 * @argv: Character array in the commandline
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
